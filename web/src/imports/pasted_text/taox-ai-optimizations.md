请基于当前已经完成的 TaoX AI 网站设计进行局部优化和修改。

重要要求：

不要重新设计整个网站。
不要更改当前的整体深色 AI SaaS 视觉风格。
不要更改现有导航结构、品牌色、基础字体、卡片语言和整体布局。
请只修改以下指定区域，并保持与现有设计系统一致。

本次主要修改以下四个部分：

1. 工具详情弹窗高度统一
2. 普通用户与付费会员状态区分
3. 会员中心中的最近使用和最近更新使用网站真实已有工具
4. 我的权益页面增加普通用户和付费会员的不同状态

--------------------------------------

一、修改所有工具详情弹窗的统一高度

目前不同工具的“欧哥点评”和“工具介绍”文字长度不同，导致不同工具详情弹窗的整体高度不一致。

例如：
Midjourney 工具详情页和 Obsidian 工具详情页，因为文字内容长度不同，导致弹窗内容区域高度不统一。

请修改为统一的固定结构。

要求：

1. 所有工具详情弹窗整体宽度统一。
2. 所有工具详情弹窗整体高度统一。
3. 弹窗顶部和浏览器视口顶部保持固定安全距离。
4. 弹窗底部和浏览器视口底部保持固定安全距离。
5. 不允许因为文字多少而改变整个详情弹窗高度。
6. 背景遮罩、圆角、关闭按钮位置全部统一。

建议布局逻辑：

工具详情弹窗：
max-height: calc(100vh - 48px)

顶部距离：
24px

底部距离：
24px

桌面端可以根据现有设计比例调整，但所有工具必须完全一致。

弹窗结构固定为：

┌──────────────────────────────┐
│                              │
│        工具展示图片           │
│        固定高度区域           │
│                              │
├──────────────────────────────┤
│ 工具分类                     │
│                              │
│ 工具名称       立即使用       │
│                              │
│ 评分 / 用户数据              │
├──────────────────────────────┤
│ 欧哥点评                     │
│ 固定高度，文字过长内部滚动    │
├──────────────────────────────┤
│ 工具介绍                     │
│ 固定高度，文字过长内部滚动    │
├──────────────────────────────┤
│ 标签区域                     │
└──────────────────────────────┘

具体交互要求：

A. 顶部工具图片

所有工具图片区域保持统一固定高度。

无论原始图片是横图、竖图还是不同尺寸，都不能改变图片区域高度。

图片使用：
object-fit: cover

或者根据图片内容使用：
object-fit: contain

但整个图片容器高度必须统一。

例如可以设计为：

桌面端图片区域高度约 430px - 480px

请根据当前弹窗宽度选择最佳比例。

图片不能因为工具介绍内容增加而压缩。

--------------------------------------

B. 欧哥点评区域

“欧哥点评”卡片保持统一高度或者最大高度。

例如：

默认展示 2-4 行内容。

文字少时：
保持正常留白，不改变整个页面高度。

文字过长时：
在点评卡片内部出现垂直滚动。

不要让点评内容撑高整个详情弹窗。

建议增加：

max-height
overflow-y: auto

滚动条设计要符合现有深色 UI 风格：
细
低对比度
不抢视觉焦点

--------------------------------------

C. 工具介绍区域

工具介绍区域同样保持固定最大高度。

默认可以展示约 4-6 行文字。

当介绍文字超过最大高度时：

只在“工具介绍”的文字内容区域内部垂直滚动。

不要使整个弹窗继续向下增长。

要求：

短介绍和长介绍的工具详情页，整体高度完全一致。

例如：

Midjourney
Obsidian
以及网站中其他工具

打开后的弹窗上下边界位置必须一致。

--------------------------------------

D. 内容滚动逻辑

请优先采用：

顶部工具图片固定
工具基本信息固定
欧哥点评内部滚动
工具介绍内部滚动

而不是让整个工具详情页面因为文字多少产生不同高度。

同时保证：

鼠标滚轮位于点评区域时滚动点评内容。

鼠标滚轮位于工具介绍区域时滚动介绍内容。

弹窗关闭按钮始终固定在右上角，不随内容滚动消失。

--------------------------------------

二、修正会员中心的普通用户状态

当前设计存在逻辑错误：

左侧账号身份显示“普通用户”，但是会员中心顶部却显示：

AI自媒体会员
状态正常
会员编号
有效期
续费会员

这种状态逻辑不一致。

请将用户状态明确区分为：

状态A：普通用户 / 非付费用户
状态B：正式付费会员

请分别设计两种状态。

--------------------------------------

A. 普通用户状态

当左侧用户身份是：

普通用户

会员中心顶部不能显示：

AI自媒体会员
状态正常
会员编号
会员有效期
续费会员

普通用户的顶部卡片应该改为：

标题：
普通用户

状态：
免费账户

说明：
当前账号尚未开通 AI 自媒体会员

辅助说明：
升级会员后可以使用会员专属工具、教程、工作流和会员服务。

主要按钮：
升级会员

次要按钮：
查看会员权益

普通用户状态不要使用皇冠会员图标。

可以使用普通用户头像或者基础账户图标。

普通用户顶部卡片示例：

┌──────────────────────────────────────┐
│ 👤 普通用户                           │
│                                      │
│ 当前状态：免费账户                   │
│ 131****2871                          │
│                                      │
│ 升级会员后解锁更多AI工具和会员服务    │
│                                      │
│ [升级会员]     [查看会员权益]         │
└──────────────────────────────────────┘

--------------------------------------

B. 正式付费会员状态

只有数据库会员状态为 active 的用户，才显示：

皇冠会员图标

AI自媒体会员

状态正常

会员编号

加入时间

有效期

续费会员

查看我的权益

正式会员卡片保持当前设计风格即可。

请在 Figma 中分别创建：

Member Center / Free User

Member Center / Paid Member

两个完整页面状态。

--------------------------------------

三、修改会员中心“最近使用”内容

当前设计中的：

短视频脚本生成器
爆款标题生成器
小红书文案助手

如果这些工具不是当前 TaoX AI 网站真实存在的工具，不要使用这些虚构名称。

请从当前网站已经存在的工具库中选择真实工具。

例如当前页面中已经存在：

Midjourney
Obsidian

以及其他当前网站工具列表中实际存在的工具。

要求：

必须读取和沿用当前网站工具卡片中真实存在的：

工具名称
工具分类
工具图片
工具图标

不要自行创造网站不存在的工具名称。

最近使用区域建议结构：

最近使用

工具真实名称
工具分类
最近使用时间
再次使用

例如：

Midjourney
图像生成
最近使用：10分钟前
[再次使用]

Obsidian
文字创作 / 知识管理
最近使用：昨天
[再次使用]

第三个工具也请选择网站当前真实存在的工具。

重要：

这里的数据是设计示例，但名称必须来自当前本地已有工具库，不要创造：

短视频脚本生成器
爆款标题生成器
小红书文案助手

除非这些工具确实存在于当前网站数据中。

--------------------------------------

四、修改“最近更新”内容

当前设计中的最近更新：

新增小红书选题分析工具
短视频脚本工具升级
新增AI内容运营教程

如果这些内容和工具在当前网站不存在，请全部替换。

最近更新必须基于当前网站真实存在的工具。

请从当前本地工具数据中选择真实工具进行示例。

例如可以使用真实工具名称，并使用以下更新类型：

某工具详情页内容更新
某工具使用教程更新
某工具分类信息优化
某工具功能说明更新
新增某个当前真实存在的工具

例如：

Midjourney 使用指南更新

Obsidian 工具详情优化

其他当前已有工具名称 + 教程更新

不要虚构不存在的工具产品。

最近更新区域格式保持：

更新事件名称
更新时间

例如：

Midjourney 使用指南更新
2天前

Obsidian 工具详情优化
5天前

所有名称优先从项目现有真实工具列表读取。

--------------------------------------

五、修改“我的权益”页面的用户状态逻辑

当前“我的权益”页面显示：

AI自媒体会员
状态正常
会员有效期365天
会员编号
加入时间
到期时间

但是如果当前用户身份是“普通用户”，这些信息不应该出现。

请分别设计两个状态。

--------------------------------------

状态1：普通用户的“我的权益”页面

顶部会员状态卡改为：

普通用户

当前状态：
免费账户

账号：
131****2871

说明：
当前尚未开通 AI 自媒体会员

按钮：
升级会员

次要按钮：
了解会员权益

不要显示：

会员编号
365天会员有效期
会员加入时间
会员到期时间
状态正常
皇冠会员标识

--------------------------------------

普通用户的权益卡片应该区分：

免费可用权益
会员专属权益

例如：

基础免费工具

状态：
可使用

按钮：
进入工具中心

--------------------------------------

全部会员AI工具

状态：
需要升级

说明：
升级会员后解锁完整会员工具权限

按钮：
升级解锁

--------------------------------------

会员课程与使用教程

状态：
需要升级

按钮：
升级解锁

--------------------------------------

会员交流社群

状态：
需要升级

按钮：
了解会员

--------------------------------------

持续更新服务

状态：
会员专属

按钮：
查看会员权益

普通用户权益页面不能出现大量绿色“已解锁”。

普通用户应该清楚看到：

哪些是当前可以使用的

哪些需要升级

同时不要做成过度促销的电商页面。

保持专业 SaaS 产品感。

--------------------------------------

状态2：付费会员的“我的权益”页面

正式付费会员可以保持：

AI自媒体会员
状态正常
会员有效期
会员编号
加入时间
到期时间

权益卡片可以显示：

已解锁
正常

按钮：

进入工具中心
开始学习
查看加入方式
查看更新日志

请建立两个完整页面 Variant：

Benefits / Free User

Benefits / Paid Member

--------------------------------------

六、数据一致性要求

请确保所有用户状态在不同页面保持一致。

例如：

如果左侧显示：

普通用户

那么：

会员中心
我的权益
顶部账号卡片

全部必须显示普通用户状态。

不能出现：

左侧普通用户
但会员中心显示AI自媒体会员

这种数据矛盾。

同样：

如果用户是付费会员：

左侧用户身份显示：
AI自媒体会员

会员中心显示：
AI自媒体会员

我的权益显示：
AI自媒体会员

三个位置保持一致。

--------------------------------------

七、工具数据一致性要求

会员中心中的：

最近使用
最近更新
推荐工具
推荐工作流

优先使用当前网站真实存在的数据。

不要重新创造当前产品不存在的工具。

设计时请从当前工具列表、工具卡片和现有本地数据中选择真实名称。

至少确保：

工具名称一致
分类一致
图片或图标一致
详情页面名称一致

例如：

工具列表卡片叫 Midjourney

最近使用中也必须叫 Midjourney

工具详情页也必须叫 Midjourney

不能出现不同名称。

--------------------------------------

八、视觉保持要求

请保持当前已有设计风格：

深色背景
紫色作为主要品牌强调色
会员使用少量金色
正常状态使用绿色
危险操作使用红色

不要修改：

左侧导航结构
整体页面宽度
内容区域位置
基础卡片圆角
现有字体体系
当前暗色视觉风格

本次重点是：

修复页面逻辑
统一详情弹窗高度
统一数据状态
使用真实工具内容
区分普通用户和付费会员

不是重新进行视觉设计。

最终请输出以下状态：

1. 固定统一高度的工具详情弹窗
2. 长点评内容滚动状态
3. 长工具介绍内容滚动状态
4. 普通用户会员中心
5. 付费会员会员中心
6. 普通用户我的权益
7. 付费会员我的权益
8. 使用真实已有工具的最近使用区域
9. 使用真实已有工具的最近更新区域

请保持当前页面已有布局，只针对以上问题做精确修改。